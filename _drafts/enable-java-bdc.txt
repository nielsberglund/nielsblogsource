# kubectl into the container which holds master.
kubectl exec -it -n sqlbdc-cluster master-0 -c mssql-server bash

# browse around and find where the extension and sdk is.
cd /opt/mssql

# look around and find it in /opt/mssql/lib

javaextension.so
mssql-java-lang-extension.jar

# copy the individual files to your local machine
kubectl cp sqlbdc-cluster/master-0:/opt/mssql/lib/javaextension.so Users/niels/javaextension.so -c mssql-server
kubectl cp sqlbdc-cluster/master-0:/opt/mssql/lib/mssql-java-lang-extension.jar Users/niels/mssql-java-lang-extension.jar -c mssql-server

# use 7zip to first tar the two files into a tar archive, the use 7zip to gz the two files

# alternatively create a directory somewhere for the files, and copy the files into that directory
# then use wsl and tar -pczf the files into a tar.gz directory
tar -pczf java-lang-extension-3.2-linux.tar.gz * 

# upload the files to the container

kubectl cp java-lang-extension-3.2-linux.tar.gz sqlbdc-cluster/master-0:opt/mssql/java/jars -c mssql-server

kubectl cp mssql-java-lang-extension.jar  sqlbdc-cluster/master-0:opt/mssql/java/jars -c mssql-server

kubectl cp sql-1.0.jar  sqlbdc-cluster/master-0:opt/mssql/java/jars -c mssql-server

