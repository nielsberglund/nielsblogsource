# grab the docker image
docker pull striim/evalversion

# start up
docker run -d -p 8080:9080 -e "STRIIM_ACCEPT_EULA=Y" striim/evalversion

# check that container is running
docker ps

# log into the docker container use the name from the ps command

docker exec -it <name_from_ps_cmd> /bin/bash

# web UI
http://localhost:8080 

admin admin

jdbc:sqlserver://[serverName[\instanceName][:portNumber]][;property=value[;property=value]]



docker cp C:\Users\nielsb\Downloads\mssql-jdbc-7.0.0.jre8.jar striim:/opt/striim/lib/